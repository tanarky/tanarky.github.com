from setuptools import setup, find_packages

setup(
    namespace_packages=['tanarky'],
    packages     = ['tanarky', 'tanarky.cookie'],
    package_dir  = {'':'src'},
    name        = 'tanarky.cookie',
    version     = '1.0',
    )
